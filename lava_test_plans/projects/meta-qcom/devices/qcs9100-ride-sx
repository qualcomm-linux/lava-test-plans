{% extends "devices/qcs9100-ride" %}

{# Use QDL deployment for this device #}
{% set deploy_method_template = "qdl.jinja2" %}

{# Device-specific test exclusions for meta-qcom project #}
{% set EXCLUDED_TESTS = ['DSP_AudioPD', 'AudioRecord', 'AudioPlayback', 'adsp_remoteproc'] %}

{% set flasher_device_type = "qcs9100-ride-sx" %}

{# Exclude unsupported testplan for this device #}
{% set EXCLUDED_TESTPLANS = ['pre-merge-camera.yaml', 'pre-merge-audio.yaml'] %}

