{% extends "devices/shikra-iqs-evk" %}

{# Device-specific test exclusions for meta-qcom project #}
{% set EXCLUDED_TESTS = ['fastrpc_test', 'AudioRecord', 'AudioPlayback', 'DSP_AudioPD', 'cdsp_remoteproc', 'adsp_remoteproc', 'WiFi_Firmware_Driver', 'WiFi_OnOff'] %}

{% set flasher_device_type = "qcs" %}

{# Exclude unsupported testplan for this device #}
{% set EXCLUDED_TESTPLANS = ['pre-merge-display-gfx.yaml', 'pre-merge-gstr-video.yaml', 'pre-merge-audio.yaml', 'pre-merge-camera.yaml'] %}